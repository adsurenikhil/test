import { MyStyleDirDirective } from './my-style-dir.directive';

describe('MyStyleDirDirective', () => {
  it('should create an instance', () => {
    const directive = new MyStyleDirDirective();
    expect(directive).toBeTruthy();
  });
});
